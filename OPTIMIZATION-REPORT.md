# تقرير تحسين السرعة واستهلاك الموارد

## النتيجة التنفيذية

تم تحسين Thanarah AI ليعمل بنموذج محلي صغير، مع افتراضات منخفضة الاستهلاك على CPU وGPU. أصبح النظام يستخدم ملفاً شخصياً سريعاً افتراضياً، وذاكرة محادثات تعتمد على الاسترجاع، وذاكرة تخزين مؤقتة للطلبات المتكررة. لا يتم إجراء Fine-tuning أو إعادة تدريب للنموذج في كل رسالة.

## القياسات المحلية

| الاختبار | النتيجة |
|---|---:|
| فحص Python | ناجح |
| بناء NestJS API | ناجح |
| بناء Next.js | ناجح |
| صحة Ollama والنموذج المحلي | ناجح |
| أول طلب سريع في آخر اختبار | نحو 413 ms |
| الطلب المتطابق بعد التخزين المؤقت | نحو 9 ms |
| عدد التضمينات المسترجعة من الذاكرة في الطلب | 3 كحد أقصى |

هذه القياسات تخص بيئة التشغيل الحالية، وقد تختلف على جهاز آخر حسب المعالج، الذاكرة، عدد الطلبات المتزامنة، وطول السؤال والرد.

## كيف يتعلم النظام

التعلم المضاف هنا هو **تعلم بالاسترجاع**. بعد نجاح الرد، تُحفظ نسخة محدودة من سؤال المستخدم والرد. عند وصول سؤال جديد، يبحث النظام عن أمثلة قريبة باستخدام مطابقة خفيفة للكلمات، ثم يضيف أفضل الأمثلة إلى السياق. عند توفر MongoDB تُخزن الذاكرة بشكل مستمر مع فهرس على المستأجر والزمن؛ وعند غياب MongoDB يستخدم النظام ذاكرة محلية محدودة.

هذا التصميم يسمح بتجميع عدد كبير من الخبرات دون تشغيل GPU أو تدريب النموذج في كل مرة. لكنه لا يغيّر أوزان النموذج الأساسية، ولا يضمن أن النموذج سيتعلم معلومة جديدة إذا لم تكن محفوظة أو مسترجعة بشكل مناسب.

## إعدادات الموارد

| الإعداد | القيمة الافتراضية | الغرض |
|---|---:|---|
| `LOCAL_AI_NUM_CTX` | `2048` | تقليل نافذة السياق والذاكرة |
| `LOCAL_AI_NUM_THREAD` | `4` | تحديد عدد خيوط CPU |
| `LOCAL_AI_NUM_BATCH` | `64` | تقليل ذروة الاستهلاك |
| `LOCAL_AI_KEEP_ALIVE` | `10m` | إبقاء النموذج محملاً وتجنب كلفة إعادة التحميل |
| `LOCAL_AI_MAX_TOKENS_FAST` | `96` | ردود قصيرة وسريعة |
| `LOCAL_AI_MAX_TOKENS_BALANCED` | `256` | الردود المعتادة |
| `LOCAL_AI_MAX_TOKENS_DEEP` | `512` | التحليل الأطول |
| `RESPONSE_CACHE_SIZE` | `512` | حفظ عدد محدود من الردود المتكررة |
| `MEMORY_RECALL_LIMIT` | `3` | منع تضخم السياق |

## ملفات التنفيذ الأساسية

تم تعديل `services/ai-engine/app/backends/ollama.py` لتطبيق إعدادات Ollama منخفضة الموارد، وتعديل `services/ai-engine/app/router/intelligence_router.py` لإضافة الملفات الشخصية، تقليص سجل المحادثة، الذاكرة، وذاكرة التخزين المؤقت. تمت إضافة `services/ai-engine/app/memory/service.py` للذاكرة المستمرة، وتعديل صفحة `settings/ai` لإتاحة اختيار **سريع** أو **متوازن** أو **تحليل عميق** وتفعيل الذاكرة لكل مستأجر.

## التشغيل

```bash
cd /home/ubuntu/ai-platformcore-thanarahsystem
./start-dev.sh
```

الوضع الموصى به للأجهزة محدودة الموارد هو `responseProfile=fast` مع النموذج المحلي `qwen2.5:0.5b`. يمكن اختيار `balanced` أو `deep` عندما تكون جودة التحليل أهم من السرعة، مع توقع استهلاك ووقت أعلى.
