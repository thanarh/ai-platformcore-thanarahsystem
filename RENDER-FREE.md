# نشر Thanarah AI على Render بأقل تكلفة

## البنية المجانية المقترحة

يستخدم المشروع ملف `render.yaml` لإنشاء Web Service واحد على خطة Render Free. الخدمة تشغّل واجهة Next.js على المنفذ العام الذي توفره Render، وNestJS API وPython AI Engine داخلياً على `127.0.0.1`. لا يتم تشغيل Ollama أو تنزيل نموذج محلي داخل Render Free لأن الخطة المجانية محدودة تقريباً بـ512MB RAM و0.1 CPU، كما أن نظام الملفات مؤقت.

يستخدم محرك الذكاء الاصطناعي على Render مسار fallback الخفيف افتراضياً. ويمكن تفعيل مزود مجاني اختياري مثل Groq أو OpenRouter من خلال مفاتيح المستخدم، مع حدود يومية ودقيقة داخلية. لا يتم تفعيل OpenAI أو Anthropic إلا إذا غيّر المستخدم `FREE_PROVIDER_ONLY=false` و`ALLOW_EXTERNAL_PROVIDERS=true` معاً، ولذلك لا يمكن أن يتحول الاستخدام إلى مدفوع بشكل افتراضي.

## تنبيه أمني قبل النشر

رابط MongoDB الذي تم إرساله أثناء إعداد المشروع يحتوي على كلمة مرور. لا تضعه في GitHub أو `.env.example` أو `render.yaml` أو أي لقطة شاشة. غيّر كلمة مرور مستخدم MongoDB من لوحة MongoDB Atlas، ثم استخدم الرابط الجديد داخل Render فقط في المتغير السري `MONGODB_URI`.

## خطوات النشر

1. ادفع المشروع إلى GitHub بعد مراجعة الأسرار، وتأكد أن ملف `.env` غير متتبع.
2. في Render اختر **New Blueprint** واربط مستودع GitHub، ثم اختر ملف `render.yaml`.
3. أدخل `MONGODB_URI` الجديد في Render كمتغير سري فقط. لا تعتمد على ملفات Render المحلية لحفظ البيانات.
4. اترك `LOCAL_AI_ENABLED=false` و`FREE_PROVIDERS_ENABLED=false` في البداية للتأكد من نجاح النشر.
5. إذا أردت مزوداً مجانياً محدوداً، أدخل `GROQ_API_KEY` أو `OPENROUTER_API_KEY`، ثم فعّل `FREE_PROVIDERS_ENABLED=true`. لا يوجد ضمان لاستخدام غير محدود، والحدود تختلف حسب الحساب والموديل.
6. اختبر الصفحة الرئيسية ثم `/api/health` عبر نفس نطاق الواجهة.

## إعدادات الحماية من التكلفة

القيم الآمنة هي:

```env
LOCAL_AI_ENABLED=false
FREE_PROVIDER_ONLY=true
FREE_PROVIDERS_ENABLED=false
ALLOW_EXTERNAL_PROVIDERS=false
```

عند تفعيل مزود مجاني، يظل كل مزود محمياً بحصة يومية وحصة طلبات في الدقيقة. عند انتهاء الحصة يرفض المحرك الطلب لذلك المزود ويعود إلى fallback بدلاً من محاولة استخدام مزود مدفوع.

## قيود مهمة

الخطة المجانية في Render مناسبة للتجربة أو المشاريع الصغيرة وليست ضماناً لخدمة إنتاجية تعمل إلى الأبد. الخدمة تتوقف بعد 15 دقيقة من عدم النشاط وتحتاج وقتاً للعودة، ونظام الملفات يفقد التغييرات عند إعادة التشغيل أو النشر. لذلك يجب حفظ MongoDB خارج نظام الملفات، وعدم تنزيل النماذج على Render Free.

للحصول على جودة عالية مجاناً، أفضل خيار عملي هو تشغيل Ollama على جهاز شخصي أو جهاز منزلي دائم وربطه بالخدمة عبر شبكة آمنة، أو تشغيل النموذج محلياً عند التطوير. أما APIs المجانية فهي مفيدة كاحتياط محدود وليست غير محدودة.
