# دخول الإدارة وهوية الموقع

## بيانات دخول الإدارة

لا يحتوي النظام على بريد أو كلمة مرور Admin افتراضيين، وهذا مقصود أمنياً. عند استخدام قاعدة بيانات فارغة، فإن **أول مستخدم يسجل من صفحة `/register` يحصل تلقائياً على دور `OWNER`**. يستخدم هذا المستخدم بريده وكلمة المرور اللذين اختارهما أثناء التسجيل للدخول من `/login`، ثم تظهر له صفحات الإدارة تحت `/admin`.

الأدوار التي تسمح للواجهة بعرض قسم الإدارة هي `OWNER` و`ADMIN` و`AI_ADMIN`. إذا كانت قاعدة البيانات تحتوي مستخدمين بالفعل، فلن يصبح أي تسجيل جديد مديراً تلقائياً؛ يجب تغيير دور الحساب المطلوب من MongoDB إلى أحد هذه الأدوار، والأفضل `OWNER` لمالك النظام أو `ADMIN` لمدير تشغيل.

لا تضف بيانات مدير ثابتة إلى GitHub أو `.env.example`. كما يجب تغيير أي كلمة مرور أو رمز وصول تم إرساله سابقاً في المحادثة.

## الأيقونة واللوجو

كانت الواجهة تشير إلى `/favicon.ico` بينما الملف غير موجود، لذلك لم يجد المتصفح أيقونة صالحة. تمت إضافة الملفات التالية من علامة Thanarah الحالية:

| الملف | الاستخدام |
|---|---|
| `public/favicon.ico` | أيقونة تبويب المتصفح والتوافق القديم |
| `public/favicon-16x16.png` و`favicon-32x32.png` | أحجام المتصفح القياسية |
| `src/app/icon.png` | أيقونة Next.js الأساسية |
| `src/app/apple-icon.png` | أجهزة Apple |
| `manifest.ts` | تثبيت الموقع كتطبيق واستخدام الأيقونات الصحيحة |
| `robots.ts` و`sitemap.ts` | مساعدة محركات البحث على اكتشاف الصفحات العامة |

تمت أيضاً إضافة Open Graph وTwitter Cards وبيانات منظمة من نوع `Organization` مع رابط اللوجو.

## إعداد النطاق

أضف في Render متغيراً عاماً باسم:

```env
NEXT_PUBLIC_SITE_URL=https://YOUR-DOMAIN
```

اكتب النطاق كاملاً ببروتوكول `https://` ومن دون شرطة مائلة أخيرة. تستخدمه الروابط القانونية وملف sitemap وبيانات اللوجو المنظمة.

## ظهور الأيقونة في Google

نشر الملفات لا يغير نتائج Google فوراً. بعد نجاح النشر، تحقق من أن الروابط التالية تفتح مباشرة:

```text
https://YOUR-DOMAIN/favicon.ico
https://YOUR-DOMAIN/icon-512.png
https://YOUR-DOMAIN/robots.txt
https://YOUR-DOMAIN/sitemap.xml
```

بعد ذلك أضف الموقع إلى Google Search Console، أرسل `/sitemap.xml`، واطلب إعادة فهرسة الصفحة الرئيسية. يحتفظ Google بنسخة مخبأة من الأيقونة، ولذلك قد يحتاج ظهور التغيير إلى إعادة زحف وليس إلى تعديل إضافي في التطبيق.
