# ترقية الذكاء الاصطناعي في Thanarah AI

تم تحويل النظام إلى بنية **محلية أولاً ومجانية افتراضياً**. الهدف هو تشغيل المحادثة والبحث الدلالي وقاعدة المعرفة دون الاعتماد على واجهات مدفوعة لكل طلب، مع إبقاء مزودي الخدمات الخارجيين كخيار اختياري فقط.

## ما تم تنفيذه

| المجال | التنفيذ |
|---|---|
| توليد النصوص | تشغيل Ollama محلياً مع النموذج المفتوح `qwen2.5:0.5b` |
| مزود احتياطي | Fallback عربي/إنجليزي يعمل عند توقف النموذج المحلي |
| التضمينات | Sentence Transformers مع `paraphrase-multilingual-MiniLM-L12-v2` |
| بديل التضمينات | Hashing embeddings مضمّنة، حتى لا يتوقف النظام عند غياب النموذج |
| RAG | قراءة النصوص وMarkdown وPDF وDOCX، ثم تقسيمها وتخزين التضمينات والبحث بالتشابه الكوني |
| حماية التكلفة | مزودو OpenAI وAnthropic لا يعملون إلا بعد تفعيل `ALLOW_EXTERNAL_PROVIDERS=true` صراحةً |
| واجهة الإدارة | صفحة إعدادات الذكاء الاصطناعي تعرض حالة التوليد المحلي والتضمينات وRAG باللغة العربية |
| قابلية التشغيل | إصلاح مسارات Ollama التي كانت مرتبطة ببيئة Replit فقط، لتعمل من مجلد المشروع الحالي |

## الملفات الرئيسية التي تغيرت

| الملف | الغرض |
|---|---|
| `services/ai-engine/app/embeddings/service.py` | خدمة التضمينات المحلية والبديل التلقائي |
| `services/ai-engine/app/rag/pipeline.py` | ربط RAG بخدمة التضمينات الجديدة |
| `services/ai-engine/app/backends/registry.py` | جعل الخدمات الخارجية اختيارية صراحةً |
| `services/ai-engine/app/routers/backends.py` | إضافة endpoint لقدرات الذكاء الاصطناعي المجانية |
| `apps/api/src/modules/ai/ai.controller.ts` | تمرير قدرات AI إلى الواجهة |
| `apps/web/src/app/(dashboard)/settings/ai/page.tsx` | لوحة حالة الحلول المجانية |
| `start-ollama.sh` | تشغيل Ollama من أي مسار دون الاعتماد على `/home/runner` |
| `services/ai-engine/requirements-optional.txt` | مكتبات Sentence Transformers وFAISS الاختيارية |

## التشغيل

```bash
cd /home/ubuntu/ai-platformcore-thanarahsystem
./start-dev.sh
```

يتم تشغيل Ollama تلقائياً من خلال `start-ollama.sh`. عند أول تشغيل، يتم تنزيل النموذج المحلي مرة واحدة ثم تخزينه في `.ollama/models`. لا توجد رسوم لكل طلب عند استخدام هذا المسار المحلي.

لتثبيت طبقة التضمينات الأقوى:

```bash
python3 -m pip install --target .pythonlibs/lib/python3.12/site-packages \
  -r services/ai-engine/requirements-optional.txt
```

## نتائج التحقق

تم التحقق بنجاح من تجميع Python، وبناء NestJS API، وبناء Next.js، واكتشاف نموذج Ollama المحلي، وتنفيذ محادثة عربية مباشرة عبر النموذج المحلي. كما تم تحميل نموذج التضمينات متعدد اللغات بنجاح واختباره على نص عربي.

## تحسين السرعة واستهلاك CPU/GPU

تمت إضافة ثلاث درجات للاستجابة: **سريع** للنصوص القصيرة، و**متوازن** للطلبات المعتادة، و**تحليل عميق** للمهام الطويلة. الوضع الافتراضي هو السريع، مع نافذة سياق محدودة، وعدد خيوط CPU مضبوط، وحجم دفعة منخفض، وإبقاء النموذج محمّلاً لفترة محددة حتى لا تتكرر كلفة التحميل.

تمت إضافة ذاكرة مستمرة خفيفة. النظام لا يعيد تدريب النموذج مع كل رسالة؛ بدلاً من ذلك يحفظ أمثلة المحادثة ويسترجع عدداً صغيراً من الأمثلة ذات الصلة. هذا يسمح بتراكم عدد كبير من الخبرات دون استهلاك GPU أثناء كل رد. كما تمت إضافة ذاكرة تخزين مؤقتة للطلبات المتطابقة، وقد تم التحقق من أن الطلب المتكرر انتقل من نحو `1669ms` إلى نحو `10ms` في الاختبار المحلي.

> لا يمكن ضمان سرعة ثابتة أو استهلاك صفري؛ الأداء يعتمد على المعالج والذاكرة وطول الطلب. النموذج المحلي الصغير أسرع وأخف، بينما النماذج الأكبر تعطي جودة أعلى وتحتاج موارد أكثر.

## التطوير الإضافي المنفّذ

أضيفت الآن آلية تقييم للردود من داخل صفحة المحادثة. يستطيع المستخدم وضع علامة إعجاب أو عدم إعجاب، وإدخال تصحيح اختياري عند عدم الرضا. تُحفظ هذه المعلومة مع الرسالة، وتُعاد الملاحظات التصحيحية إلى سياق المحادثات اللاحقة دون إعادة تدريب النموذج.

كما تم تحسين RAG ليستخدم ترتيباً هجينا يجمع بين التشابه الدلالي وتطابق الكلمات العربية والإنجليزية، مع تنظيف أفضل للنص وتقليل حجم القطع. أصبح البحث محدوداً بعدد أقصى من القطع وبمهلة زمنية حتى لا يسبب استهلاكاً كبيراً عند نمو قاعدة المعرفة، مع إضافة فهارس MongoDB للمستأجر والمصدر.

## المرحلة التالية المقترحة

يمكن توسيع النظام بعد ذلك بإضافة نماذج محلية أكبر مثل Qwen أو Llama أو Gemma حسب ذاكرة الجهاز، وإضافة FAISS فعلياً للفهرسة واسعة النطاق، وإضافة OCR للصور والمستندات الممسوحة، وأدوات محلية آمنة مثل تلخيص الملفات واستخراج الجداول وإنشاء التقارير. هذه الإضافات يمكن أن تبقى مجانية بالكامل عند تشغيل النماذج محلياً، لكن سرعتها تعتمد على ذاكرة الجهاز ووجود GPU.

## مراجع تقنية

[1]: https://docs.ollama.com/api/introduction "Ollama API Introduction"
[2]: https://sbert.net/ "Sentence Transformers Documentation"
[3]: https://faiss.ai/index.html "Faiss Documentation"
[4]: https://github.com/pgvector/pgvector "pgvector Repository"
